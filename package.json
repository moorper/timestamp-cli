{
  "name": "timestamp-cli",
  "version": "1.0.0",
  "description": "Print current unix timestamp.",
  "author": "Where",
  "license": "MIT",
  "scripts": {
    "dev": "timestamp",
    "build": "babel src -d dist",
    "lint": "standard src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "timestamp": "dist/index.js"
  },
  "dependencies": {
    "chalk": "^2.4.2",
    "commander": "^2.19.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "standard": "^12.0.1"
  }
}
